[build]
  publish = "public"
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Every 30 minutes check alerts (configurable in function)
[[scheduled.functions]]
  name = "alerts"
  cron = "*/30 * * * *"
